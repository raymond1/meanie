To start the server type in npm start.
Use the command 'gulp build' to compile the website. Basically, this command runs ng build and then copies the resulting angular tags into the input_output.pug file where it is used.
